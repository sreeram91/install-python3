name: install nginx
on:
  push:
    branches:
      - master
    
jobs:
  check-nginx-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-nginx@v3
        with:
          nginx-version: '1.21.6'

      - run: apt install nginx -y

      - run: nginx --version
